This project provides a simple user account system where users can register and log in. It uses JWT (JSON Web Tokens) for authentication to ensure that only authenticated users can access protected routes. The token confirms that the user making the request is the same user who logged in.

Password hashing with bcrypt

Middleware for token verification

Express.js error handling

User model with Mongoose 

Cookie Parser for parsing cookies and cookies information
